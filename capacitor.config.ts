import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
    appId: 'com.marcadorgeneral.app',
    appName: 'marcador-general',
    webDir: 'dist',
};

export default config;
